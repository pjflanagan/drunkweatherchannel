import React, { FC } from "react"

import { MainComponent } from 'src/components';

import './reset.scss';
import './style.scss';

const PageIndex: FC = () => {
  return (
    <MainComponent />
  );
}

export default PageIndex;
